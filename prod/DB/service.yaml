apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: prod
  labels:
    app: mariadb
spec:
  ports:
    - name: mysql 
      port: 3306
      targetPort: 3306
  selector:
    app: mariadb